<Window x:Class="Bakery.WPFApplication.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Email:" Grid.Row="0"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="0" Margin="0,20,0,0" />

        <TextBlock Text="Password:" Grid.Row="1" Margin="0,10,0,0"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="0,30,0,0" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,50,0,0">
            <Button x:Name="LoginButton" Content="Login" Width="90" Click="LoginButton_Click"/>
            <Button x:Name="OpenRegisterButton" Content="Register" Width="90" Margin="10,0,0,0" Click="OpenRegisterButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
