<Window x:Class="Bakery.WPFApplication.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Register" Height="400" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="User name:" Grid.Row="0"/>
        <TextBox x:Name="UserNameTextBox" Grid.Row="0" Margin="0,20,0,0" />

        <TextBlock Text="Email:" Grid.Row="1" Margin="0,10,0,0"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="1" Margin="0,30,0,0" />

        <TextBlock Text="Password:" Grid.Row="2" Margin="0,10,0,0"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Margin="0,30,0,0" />

        <TextBlock Text="Confirm Password:" Grid.Row="3" Margin="0,10,0,0"/>
        <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="3" Margin="0,30,0,0" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,30,0,0">
            <Button x:Name="RegisterButton" Content="Register" Width="90" Click="RegisterButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="90" Margin="10,0,0,0" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
